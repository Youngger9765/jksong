<h1>Index</h1>

<%= link_to "新法案", new_vote_path,:class => "btn btn-primary"%>


<table class="table">
  <tr>
    <td><%= "法案內容" %></td>
  </tr>

  <% @votes.each do |v| %>
    <tr>
      <td><%= link_to v.content, vote_path(v) %></td>
      
    </tr>
  <% end %>

</table>


